# This is a minimal git configuration for agent-managed environments.
# To keep this slim and non-interactive, do NOT add editor, pager, or other UI-related settings here.
# Those should be handled via environment variables in the bootstrap scripts.
#
# This config should NOT inherit from the system config (GIT_CONFIG_NOSYSTEM=1 is set in bootstrap).

[user]
  name       = Michael Bianco
  email      = mike@mikebian.co
  signingkey = F8C4375D734A914F075A607AE5E32D3B0D0B4261

[credential]
  helper = osxkeychain

[gpg]
  program = /opt/homebrew/bin/gpg

[core]
  whitespace = fix,space-before-tab,tab-in-indent,trailing-space
