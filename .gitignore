.DS_Store
.ipython_history

# only for dotfiles repo
/.extra

/.gitconfig-local

# which vault [[ssh-keys]] are in
/.config/1Password

# personal ssh config
/.ssh/config_extra
/.ssh/vscode_config
/.ssh/op

/.claude/settings.local.json
