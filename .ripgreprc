# include line + column number with the file name
--vimgrep

# vimgrep kills coloring, let's add it back in
--color=auto

--smart-case

# if you hit a minified file, avoid outputting a mess in your terminal
--max-columns=150
